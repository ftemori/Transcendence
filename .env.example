# JWT Secret for authenticationAdd a comment on lines R1 to R3Add diff commentMarkdown input:  edit mode selected.WritePreviewAdd a suggestionHeadingBoldItalicQuoteCodeLinkUnordered listNumbered listTask listMentionReferenceSaved repliesAdd FilesPaste, drop, or click to add filesCancelCommentStart a reviewReturn to code
# Generate with: openssl rand -base64 32
JWT_SECRET=your_secret_key_here

# Environment settings
NODE_ENV=production

# Service URLs (internal Docker URLs)
AUTH_URL=http://auth-service:4000
USER_URL=http://user-service:5100
BACKEND_URL=http://backend:3000

# Public domain
DOMAIN=your-domain.com

AVAX_RPC_URL=https://api.avax-test.network/ext/bc/C/rpc

PRIVATE_KEY=

TOURNAMENT_CONTRACT_ADDRESS=